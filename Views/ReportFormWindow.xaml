<Window x:Class="AppOrderNilon.Views.ReportFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sửa Báo cáo" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="SỬA BÁO CÁO" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Report Type -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Loại báo cáo:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cboReportType" Grid.Column="1" 
                      Margin="10,0,0,0" Padding="5">
                <ComboBoxItem Content="Doanh thu" Tag="Revenue"/>
                <ComboBoxItem Content="Tồn kho" Tag="Inventory"/>
                <ComboBoxItem Content="Sản phẩm bán chạy" Tag="BestSelling"/>
            </ComboBox>
        </Grid>

        <!-- Start Date -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Từ ngày:" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpStartDate" Grid.Column="1" 
                        Margin="10,0,0,0"/>
        </Grid>

        <!-- End Date -->
        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Đến ngày:" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpEndDate" Grid.Column="1" 
                        Margin="10,0,0,0"/>
        </Grid>

        <!-- Generated Date (Read-only) -->
        <Grid Grid.Row="4" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Ngày tạo:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtGeneratedDate" Grid.Column="1" 
                     Margin="10,0,0,0" Padding="5"
                     IsReadOnly="True" Background="LightGray"/>
        </Grid>

        <!-- Report Data -->
        <Grid Grid.Row="5" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Dữ liệu báo cáo:" VerticalAlignment="Top" Margin="0,5,0,5"/>
            <TextBox x:Name="txtReportData" Grid.Row="1" 
                     Margin="0,5,0,0" Padding="5"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     FontFamily="Consolas" FontSize="11"
                     VerticalScrollBarVisibility="Auto"/>
        </Grid>

        <!-- Buttons -->
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnSave" Grid.Column="1" Content="Lưu" 
                    Width="80" Margin="0,0,10,0" Click="BtnSave_Click"/>

            <Button x:Name="btnCancel" Grid.Column="2" Content="Hủy" 
                    Width="80" Click="BtnCancel_Click"/>
        </Grid>
    </Grid>
</Window> 