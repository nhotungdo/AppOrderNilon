<Window x:Class="AppOrderNilon.Views.StaffTaskManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản lý nhiệm vụ - Staff" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="8" 
                          Direction="270" 
                          ShadowDepth="2" 
                          Color="#22000000"/>
        
        <Style x:Key="StaffButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2196F3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Quản lý nhiệm vụ" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock Text="Quản lý và theo dõi nhiệm vụ được giao" 
                               FontSize="14" 
                               Foreground="White" 
                               Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Nhân viên: " 
                               Foreground="White" 
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtStaffName" 
                               Text="Trần Nhân Viên" 
                               Foreground="White" 
                               FontWeight="Bold" 
                               VerticalAlignment="Center"/>
                    
                    <Button Content="Quay lại" 
                            Background="Transparent" 
                            Foreground="White" 
                            BorderBrush="White" 
                            BorderThickness="1"
                            Padding="10,5"
                            Margin="15,0,0,0"
                            Click="Back_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Filter and Actions -->
            <Border Grid.Row="0" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Effect="{StaticResource CardShadow}"
                    Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="txtSearch" 
                             Grid.Column="0"
                             Margin="0,0,10,0"
                             TextChanged="Search_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="Tìm kiếm nhiệm vụ..." 
                                                                   Foreground="Gray" 
                                                                   FontStyle="Italic"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    
                    <ComboBox x:Name="cmbStatusFilter" 
                              Grid.Column="1"
                              Width="150"
                              Margin="0,0,10,0"
                              SelectionChanged="StatusFilter_SelectionChanged">
                        <ComboBoxItem Content="Tất cả trạng thái"/>
                        <ComboBoxItem Content="Chờ thực hiện"/>
                        <ComboBoxItem Content="Đang thực hiện"/>
                        <ComboBoxItem Content="Hoàn thành"/>
                    </ComboBox>
                    
                    <Button Content="Làm mới" 
                            Grid.Column="2"
                            Style="{StaticResource StaffButton}"
                            Height="35"
                            Padding="15,5"
                            Margin="0,0,10,0"
                            Click="Refresh_Click"/>
                    
                    <Button Content="+ Thêm nhiệm vụ" 
                            Grid.Column="3"
                            Style="{StaticResource StaffButton}"
                            Height="35"
                            Padding="15,5"
                            Click="AddTask_Click"/>
                </Grid>
            </Border>
            
            <!-- Tasks List -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Danh sách nhiệm vụ" 
                               FontSize="18" 
                               FontWeight="Bold" 
                               Margin="0,0,0,15"/>
                    
                    <DataGrid x:Name="dgTasks" 
                              Grid.Row="1"
                              AutoGenerateColumns="False" 
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeaderWidth="0"
                              SelectionMode="Single"
                              SelectionChanged="Tasks_SelectionChanged">
                        
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên nhiệm vụ" 
                                                Binding="{Binding TaskName}" 
                                                Width="200"/>
                            <DataGridTextColumn Header="Mô tả" 
                                                Binding="{Binding Description}" 
                                                Width="250"/>
                            <DataGridTextColumn Header="Hạn" 
                                                Binding="{Binding DueDate, StringFormat=dd/MM/yyyy HH:mm}" 
                                                Width="120"/>
                            <DataGridTextColumn Header="Ưu tiên" 
                                                Binding="{Binding Priority}" 
                                                Width="80"/>
                            <DataGridTextColumn Header="Trạng thái" 
                                                Binding="{Binding Status}" 
                                                Width="100"/>
                            <DataGridTemplateColumn Header="Hành động" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Cập nhật" 
                                                    Style="{StaticResource StaffButton}"
                                                    Height="25" 
                                                    Padding="8,2"
                                                    Margin="0,0,5,0"
                                                    Click="UpdateTask_Click"/>
                                            <Button Content="Xóa" 
                                                    Background="#F44336"
                                                    Foreground="White"
                                                    Height="25" 
                                                    Padding="8,2"
                                                    Click="DeleteTask_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window> 