<Window x:Class="AppOrderNilon.Views.CategoryManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản lý Danh mục" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="QUẢN LÝ DANH MỤC" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Search and Controls -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearch" Grid.Column="0" 
                     Margin="0,0,10,0" Padding="5"
                     TextChanged="TxtSearch_TextChanged"
                     ToolTip="Tìm kiếm theo tên danh mục..."/>

            <Button x:Name="btnAdd" Grid.Column="1" Content="Thêm mới" 
                    Width="100" Margin="0,0,10,0" Click="BtnAdd_Click"/>

            <Button x:Name="btnEdit" Grid.Column="2" Content="Sửa" 
                    Width="80" Margin="0,0,10,0" Click="BtnEdit_Click"/>

            <Button x:Name="btnDelete" Grid.Column="3" Content="Xóa" 
                    Width="80" Click="BtnDelete_Click"/>
        </Grid>

        <!-- DataGrid -->
        <DataGrid x:Name="dgCategories" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionChanged="DgCategories_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="50"/>
                <DataGridTextColumn Header="Tên danh mục" Binding="{Binding CategoryName}" Width="200"/>
                <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Số SP" Binding="{Binding Quantity}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status -->
        <TextBlock x:Name="txtStatus" Grid.Row="3" 
                   Margin="0,10,0,0" 
                   Text="Sẵn sàng" 
                   FontStyle="Italic"/>
    </Grid>
</Window> 