<Window x:Class="AppOrderNilon.Views.CreateOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tạo đơn hàng mới - AppOrderNilon" 
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <DropShadowEffect x:Key="CardShadow" 
                          BlurRadius="8" 
                          Direction="270" 
                          ShadowDepth="2" 
                          Color="#22000000"/>
        
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#4CAF50"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2E7D32"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style x:Key="GroupBoxStyle" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                           Text="📋 Tạo đơn hàng mới" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnBack" 
                            Content="⬅️ Quay lại" 
                            Style="{StaticResource ModernButton}"
                            Background="#2196F3"
                            Click="btnBack_Click"
                            Margin="0,0,10,0"/>
                    <Button x:Name="btnCancel" 
                            Content="❌ Hủy" 
                            Style="{StaticResource DangerButton}"
                            Click="btnCancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left Column - Order Details -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <!-- Customer Information -->
                    <GroupBox Header="👤 Thông tin khách hàng" Style="{StaticResource GroupBoxStyle}">
                        <StackPanel>
                            <TextBlock Text="Chọn khách hàng:" FontSize="12" FontWeight="Medium" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cboCustomer" 
                                      Style="{StaticResource ModernComboBox}"
                                      SelectionChanged="cboCustomer_SelectionChanged"
                                      Margin="0,0,0,10"/>
                            
                            <Button x:Name="btnAddCustomer" 
                                    Content="➕ Thêm khách hàng mới" 
                                    Style="{StaticResource SuccessButton}"
                                    Click="btnAddCustomer_Click"
                                    HorizontalAlignment="Left"
                                    Margin="0,0,0,15"/>
                            
                            <TextBlock Text="Địa chỉ giao hàng:" FontSize="12" FontWeight="Medium" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtDeliveryAddress" 
                                     Style="{StaticResource ModernTextBox}"
                                     Height="60" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"
                                     ToolTip="Nhập địa chỉ giao hàng chi tiết"/>
                        </StackPanel>
                    </GroupBox>
                    
                    <!-- Product Selection -->
                    <GroupBox Header="📦 Chọn sản phẩm" Style="{StaticResource GroupBoxStyle}">
                        <StackPanel>
                            <!-- Product Search and Add -->
                            <Grid Margin="0,0,0,10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="Tìm kiếm sản phẩm:" FontSize="12" FontWeight="Medium" Margin="0,0,0,5"/>
                                <TextBox Grid.Row="1" 
                                         x:Name="txtProductSearch" 
                                         Style="{StaticResource ModernTextBox}"
                                         Text=""
                                         TextChanged="txtProductSearch_TextChanged"
                                         ToolTip="Nhập tên sản phẩm để tìm kiếm"
                                         Margin="0,0,0,10"/>
                                
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ComboBox Grid.Column="0" 
                                              x:Name="cboProduct" 
                                              Style="{StaticResource ModernComboBox}"
                                              Margin="0,0,10,0"
                                              ToolTip="Chọn sản phẩm từ danh sách"/>
                                    
                                    <TextBox Grid.Column="1" 
                                             x:Name="txtQuantity" 
                                             Style="{StaticResource ModernTextBox}"
                                             Text="1"
                                             Margin="0,0,10,0"
                                             ToolTip="Nhập số lượng sản phẩm"/>
                                    
                                    <Button Grid.Column="2" 
                                            x:Name="btnAddProduct" 
                                            Content="➕ Thêm" 
                                            Style="{StaticResource SuccessButton}"
                                            Click="btnAddProduct_Click"
                                            ToolTip="Thêm sản phẩm vào đơn hàng"/>
                                </Grid>
                            </Grid>
                            
                            <!-- Selected Products List -->
                            <TextBlock Text="Sản phẩm đã chọn:" FontSize="12" FontWeight="Medium" Margin="0,0,0,5"/>
                            <DataGrid x:Name="dgSelectedProducts" 
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      AlternatingRowBackground="#F8F9FA"
                                      Height="250">
                                
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPrice, StringFormat=N0 VNĐ}" Width="120"/>
                                    <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="80"/>
                                    <DataGridTextColumn Header="Thành tiền" Binding="{Binding Subtotal, StringFormat=N0 VNĐ}" Width="120"/>
                                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="150"/>
                                    
                                    <DataGridTemplateColumn Header="Thao tác" Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="🗑️" 
                                                        Style="{StaticResource DangerButton}"
                                                        Padding="8,4"
                                                        Click="btnRemoveProduct_Click"
                                                        ToolTip="Xóa sản phẩm khỏi đơn hàng"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </GroupBox>
                    
                    <!-- Order Notes -->
                    <GroupBox Header="📝 Ghi chú đơn hàng" Style="{StaticResource GroupBoxStyle}">
                        <TextBox x:Name="txtOrderNotes" 
                                 Style="{StaticResource ModernTextBox}"
                                 Height="80" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True"
                                 ToolTip="Nhập ghi chú cho đơn hàng (tùy chọn)"/>
                    </GroupBox>
                </StackPanel>
                
                <!-- Right Column - Order Summary -->
                <Border Grid.Column="1" 
                        Background="White" 
                        CornerRadius="10" 
                        Padding="20"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="💰 Tổng quan đơn hàng" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,20"
                                   Foreground="#2196F3"/>
                        
                        <!-- Order Summary -->
                        <StackPanel>
                            <TextBlock Text="Tổng tiền hàng:" FontSize="12" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtSubtotal" Text="0 VNĐ" FontSize="16" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                            
                            <TextBlock Text="Giảm giá (%):" FontSize="12" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtDiscountPercent" 
                                     Style="{StaticResource ModernTextBox}"
                                     Text="0"
                                     TextChanged="txtDiscountPercent_TextChanged"
                                     ToolTip="Nhập phần trăm giảm giá (0-100)"
                                     Margin="0,0,0,10"/>
                            
                            <TextBlock Text="Tiền giảm giá:" FontSize="12" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtDiscountAmount" Text="0 VNĐ" FontSize="12" Foreground="#FF9800" Margin="0,0,0,15"/>
                            
                            <Border Background="#F5F5F5" Padding="15" CornerRadius="5" Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="Tổng cộng:" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="txtTotalAmount" Text="0 VNĐ" FontSize="18" FontWeight="Bold" Foreground="#4CAF50"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Quick Actions -->
                            <StackPanel>
                                <Button x:Name="btnPreview" 
                                        Content="👁️ Xem trước đơn hàng" 
                                        Style="{StaticResource ModernButton}"
                                        Background="#9C27B0"
                                        Click="btnPreview_Click"
                                        Margin="0,0,0,10"
                                        ToolTip="Xem trước thông tin đơn hàng"/>
                                
                                <Button x:Name="btnSaveOrder" 
                                        Content="💾 Lưu đơn hàng" 
                                        Style="{StaticResource SuccessButton}"
                                        Click="btnSaveOrder_Click"
                                        Margin="0,0,0,10"
                                        ToolTip="Lưu đơn hàng vào hệ thống"/>
                                
                                <Button x:Name="btnSaveAndPrint" 
                                        Content="🖨️ Lưu và in hóa đơn" 
                                        Style="{StaticResource ModernButton}"
                                        Background="#FF9800"
                                        Click="btnSaveAndPrint_Click"
                                        ToolTip="Lưu đơn hàng và in hóa đơn"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window> 